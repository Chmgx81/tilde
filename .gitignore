# Binaries and build output (exact names only — tilde-*.md specs stay tracked)
/tilde
*.test
*.out

# Go
*.coverprofile
coverage.html

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Local scratch (never commit session data)
/tmp/
