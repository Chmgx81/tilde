dist/
node_modules/
.astro/

# Generated by scripts/prepare-docs.mjs on every build (from ../docs/).
# Not tracked so a docs edit doesn't show up twice and the copies can't drift.
src/content/docs/docs/
